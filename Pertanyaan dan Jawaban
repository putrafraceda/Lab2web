1. Lakukan eksperimen dengan mengubah dan menambah properti dan nilai pada kode CSS
dengan mengacu pada CSS Cheat Sheet yang diberikan pada file terpisah dari modul ini.
2. Apa perbedaan pendeklarasian CSS elemen h1 {...} dengan #intro h1 {...}? berikan
penjelasannya!
3. Apabila ada deklarasi CSS secara internal, lalu ditambahkan CSS eksternal dan inline CSS pada
elemen yang sama. Deklarasi manakah yang akan ditampilkan pada browser? Berikan
penjelasan dan contohnya!
4. Pada sebuah elemen HTML terdapat ID dan Class, apabila masing-masing selector tersebut
terdapat deklarasi CSS, maka deklarasi manakah yang akan ditampilkan pada browser?
Berikan penjelasan dan contohnya! ( <p id="paragraf-1" class="text-paragraf"> )

#Jawaban

2.  h1 {...} menargetkan semua elemen <h1> pada halaman.
    intro h1 {...} menargetkan hanya elemen <h1> yang merupakan turunan dari elemen dengan ID intro.
3.  Ketika beberapa deklarasi CSS diterapkan pada elemen yang sama, 
    browser mengikuti urutan prioritas tertentu untuk menentukan gaya mana yang akan diterapkan. Urutan prioritas adalah sebagai berikut:
    - Gaya Inline (prioritas tertinggi)
    - Gaya Internal (prioritas menengah)
    - Gaya Eksternal (prioritas terendah)
    contoh ;
    File CSS eksternal (styles.css) yang mendefinisikan h1 { color: green; }
    Gaya internal yang mendefinisikan h1 { color: blue; }
    Gaya inline yang mendefinisikan h1 { color: red; }
4.
